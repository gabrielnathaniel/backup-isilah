import 'package:flame/components.dart';

class EnemyHappy extends SpriteAnimationComponent {
  EnemyHappy({
    SpriteAnimation? animation,
    Vector2? position,
    Anchor? anchor,
  }) : super(animation: animation, position: position, anchor: anchor);
}
